#include <bits/stdc++.h>
using namespace std;
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    // Read all input and ignore
    string line;
    while (getline(cin, line)) {}
    return 0;
}